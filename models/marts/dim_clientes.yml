models:
  - name: dim_clientes
    description: Modelo dimensão de clientes
    columns:
      - name: pk_cod_cliente
        description: Chave primária do cliente
        data_tests:
          - unique
          - not_null